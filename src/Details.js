import React from 'react'

function Details(props) {
    return (
        <div>
            Hey hai i am {props.name}. I am a student in {props.clg} 
        </div>
    );
}

export default Details
